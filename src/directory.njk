---
layout: base.njk
permalink: /directory/
variant: directory
title: "Directory"
description: "Browse local businesses across St. Catharines & the Benchlands."
stickyCtaLabel: "Back to fall itineraries"
stickyCtaHref: "/couples/#itineraries"
headerCtaLabel: "Plan your fall visit"
headerCtaHref: "/couples/"
headerHomeHref: "/"
anchors:
  - { label: "Listings", href: "#listings" }
  - { label: "FAQ", href: "#faq" }
faqKey: default
---
<section class="section" id="listings" data-module="directory">
  <div class="wrap">
    <div class="section__head">
      <h2><span class="rule-l"></span>{{ site.name }} directory</h2>
      <p>Browse places to eat, sip, stay and explore across the bench.</p>
      {# Builder note: this internal directory page builds but is not linked in nav while
         featureFlags.directoryPage = false; every "directory" CTA site-wide points at
         site.directoryUrl. Flip the flag / change directoryUrl to switch. See README. #}
    </div>

    {# --- Whereabouts listings module (lazy embed slot) --- #}
    <div data-embed="whereabouts-listings">
      {# Builder note: paste the listings embed into embeds.whereabouts.listingsEmbed — loads lazily. #}
      <div class="embed-slot" style="min-height:340px">
        <div>
          <strong>The business directory</strong>
          [PLACEHOLDER — live listings ship with launch]
        </div>
      </div>
      {% if embeds.whereabouts.listingsEmbed %}
      <template>{{ embeds.whereabouts.listingsEmbed | safe }}</template>
      {% endif %}
    </div>
  </div>
</section>

{% include "partials/faq.njk" %}
{% include "partials/newsletter-inline.njk" %}

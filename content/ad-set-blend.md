# Ad-set blend per audience — DROP THE REAL DOC HERE  ⚠︎ PLACEHOLDER

This informs the final hero **headline copy** per audience. Loud & Clear is
synthesising one message per audience from the ad sets. This file was **not**
attached to the build request.

Headlines/subs are single-variable swaps in each page's front-matter:

| Page          | File                | Front-matter fields to change |
|---------------|---------------------|-------------------------------|
| Couples       | `src/couples.njk`   | `headline`, `sub`             |
| Families      | `src/families.njk`  | `headline`, `sub`             |
| Events        | `src/events.njk`    | `headline`, `sub`             |
| Stories (LP)  | `src/stories.njk`   | `headline`, `sub`             |

The kicker line — "St. Catharines & the Benchlands" — is fixed site-wide in
`src/_data/site.js` (`kicker`) and must remain the first line of copy on every page.

## Current placeholder headlines

- Couples: "Your fall wine weekend starts on the bench"
- Families: "Fall colours, farms & big days out"
- Events:  "See what's on this fall"
- Stories: "Fall stories from the bench"
